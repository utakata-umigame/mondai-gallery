version: "2"
services:
  app:
    container_name: app
    environment:
      - MONGODB_URI=mongodb://192.168.99.100:27017
    restart: always
    build: .
    ports:
      - "49160:3000"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
